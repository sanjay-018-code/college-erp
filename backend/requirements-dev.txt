# Dev/test-only dependencies. NOT installed in the production Docker image
# (see Dockerfile - only requirements.txt is installed there).
-r requirements.txt
pytest==8.3.2
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.1
