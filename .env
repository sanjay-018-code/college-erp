# Copy this to .env in the project root. docker-compose reads this
# automatically for the ${POSTGRES_*} substitutions in docker-compose.yml.
# These values MUST match backend/.env.
POSTGRES_DB=college_erp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Sanjay123
